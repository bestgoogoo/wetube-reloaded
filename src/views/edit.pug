extends base.pug

block content
  h4 Edit Title of Video
  form(action="/save-change")
    input(name="title", type="text", placeholder="New Title", value=video.title, required)
    input(type="submit", value="Save")